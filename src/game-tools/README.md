# Game Tools

Future purpose-built helpers belong in self-contained folders under this
directory and register through `registry.ts`. They may own custom UI, rules,
randomization, and persistence choices.

Do not branch on a configured game's name inside the generic randomizer.

